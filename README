Install script to quickly set up an Ubuntu box for a PHP-Drupal Programmer.

Open it up, remove all the software that you do not need and finally run it with the following command:

  sudo sh installer.sh
