Install script that sets up an Ubuntu box for a PHP-Drupal Programmer.

Open it up, remove all the software that you do not need and run it from your home directory with the following command:

sh installer.sh

It will ask you for sudo permission when needed.
